/** Named visual presets. `default` is the neutral app chrome. */
export type ThemePreset = 'default' | 'medieval';

/** Campaign value: `default` means inherit the signed-in user's account theme. */
export type CampaignTheme = 'default' | ThemePreset;
